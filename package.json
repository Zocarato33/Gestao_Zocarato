{
  "name": "gestao-demandas",
  "version": "1.0.0",
  "description": "Sistema de gestão de clientes e demandas",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "criar-admin": "node --env-file-if-exists=.env scripts/criar-admin.js",
    "test": "node --test test/api.test.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.23.0"
  }
}
